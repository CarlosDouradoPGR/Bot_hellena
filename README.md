# Kisoft Pick by Light Dashboard

## 🔧 Solução de Problemas

Se encontrar erro de `distutils`, tente:

1. **Localmente**:
   ```bash
   pip install --upgrade pip setuptools wheel
   pip install -r requirements.txt
